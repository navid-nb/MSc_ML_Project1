[project]
name = "MATH60610A-portfolio-backtesting"
version = "0.1.0"
description = "simple backtesting app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.black]
line-length = 100

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 100
